* DONE - Create a `ITypeReference` interface
* DONE - Create a `ModelContext`, similar to Rocks
* DONE - Have a `Initialize()` method on `TypeReferenceModel`, which will also need to be moved as a private, nested class of `ModelContext`.
* Integration

TODO
* Add an analyzer in Transpire to warn about recurive unions that have only 1 union case each.