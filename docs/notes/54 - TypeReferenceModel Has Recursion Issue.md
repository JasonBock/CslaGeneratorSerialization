* Create a `ITypeReference` interface
* Create a `ModelContext`, similar to Rocks
* Have a `Initialize()` method on `TypeReferenceModel`, which will also need to be moved as a private, nested class of `ModelContext`.